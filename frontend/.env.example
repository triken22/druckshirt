# Environment variables for local frontend development
# Copy this file to .env and fill in your actual values (use test/staging keys locally)

# PostHog (Frontend)
VITE_POSTHOG_KEY=<REPLACE_WITH_YOUR_STAGING_POSTHOG_PROJECT_API_KEY>
VITE_POSTHOG_HOST=https://app.posthog.com # Or your self-hosted instance

# Add other frontend-specific vars if needed
# Example: API base URL for local testing if not using relative paths
# VITE_API_BASE_URL=http://127.0.0.1:8787 